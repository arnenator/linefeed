[package]
name = "lsp"
version = "0.1.0"
edition = "2024"

[dependencies]
tower-lsp = "0.20.0"
tokio = { version = "1", features = ["full"] }
linefeed = { path = "../linefeed" }
chumsky = { git = "https://github.com/zesterer/chumsky.git", branch = "main", features = ["memoization"] }
